name: Deploy to Production

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --production

      - name: Build CSS
        run: npm run build:css || echo "Build CSS optional"

      - name: Create .env file
        run: |
          cat > .env << EOF
          NODE_ENV=production
          PORT=3000
          HOST=0.0.0.0

          # URLs
          APP_URL=https://atenmed.com.br
          CORS_ORIGIN=https://atenmed.com.br,https://www.atenmed.com.br

          # MongoDB
          MONGODB_URI=${{ secrets.MONGODB_URI }}

          # JWT
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          JWT_EXPIRES_IN=7d

          # Email (AWS SES)
          EMAIL_HOST=${{ secrets.EMAIL_HOST }}
          EMAIL_PORT=587
          EMAIL_USER=${{ secrets.EMAIL_USER }}
          EMAIL_PASS=${{ secrets.EMAIL_PASS }}
          EMAIL_FROM=${{ secrets.EMAIL_FROM }}

          # WhatsApp
          WHATSAPP_API_URL=https://graph.facebook.com/v18.0
          WHATSAPP_PHONE_ID=${{ secrets.WHATSAPP_PHONE_ID }}
          WHATSAPP_TOKEN=${{ secrets.WHATSAPP_TOKEN }}
          WHATSAPP_VERIFY_TOKEN=${{ secrets.WHATSAPP_VERIFY_TOKEN }}
          WHATSAPP_APP_SECRET=${{ secrets.WHATSAPP_APP_SECRET }}

          # Google Calendar
          GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REDIRECT_URL=https://atenmed.com.br/api/auth/google/callback

          # Security
          BCRYPT_ROUNDS=12
          SESSION_SECRET=${{ secrets.SESSION_SECRET }}

          # Features
          ENABLE_REMINDERS=true
          ENABLE_WAITLIST=true
          ENABLE_WHATSAPP=true
          ENABLE_GOOGLE_CALENDAR=true
          EOF

      - name: Deploy to Server
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          port: ${{ secrets.EC2_PORT || 22 }}
          script: |
            set -e
            echo "🔍 Checking directory..."

            # Forçar usar /var/www/atenmed
            if [ ! -d "/var/www/atenmed" ]; then
              echo "⚠️ /var/www/atenmed não existe, tentando criar..."
              sudo mkdir -p /var/www/atenmed
              sudo chown -R $USER:$USER /var/www/atenmed
            fi

            cd /var/www/atenmed
            echo "📂 Diretório atual: $(pwd)"
            echo "🔍 Branch atual antes do pull: $(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo 'não é um repo git')"
            echo "🔍 Commit atual: $(git rev-parse --short HEAD 2>/dev/null || echo 'não encontrado')"

            # Garantir que está na branch main
            if [ -d ".git" ]; then
              git fetch origin
              git checkout main 2>/dev/null || git checkout master 2>/dev/null || echo "Já na branch correta"
              git pull origin main || git pull origin master
            else
              echo "⚠️ Não é um repo git, clonando..."
              cd /var/www
              sudo rm -rf atenmed
              git clone https://github.com/imaxcodeco/atenmed-website.git atenmed
              cd atenmed
              git checkout main || git checkout master
              sudo chown -R $USER:$USER .
            fi

            echo "✅ Commit após pull: $(git rev-parse --short HEAD)"

            # Backup
            mkdir -p backups
            cp .env backups/.env.$(date +%Y%m%d_%H%M%S) 2>/dev/null || true

            # Install dependencies
            npm install --production --legacy-peer-deps --ignore-scripts

            # Build
            npm run build:css || echo "Build optional"

            # Restart with PM2
            pm2 restart atenmed --update-env || pm2 start ecosystem.config.js --env production
            pm2 save

            # Show status
            pm2 status

            echo "✅ Deploy completed successfully!"

      - name: Health Check
        run: |
          sleep 10
          curl --fail https://atenmed.com.br/health || exit 1
          echo "✅ Health check passed!"

      - name: Notify Success
        if: success()
        run: |
          echo "🎉 Deploy to atenmed.com.br successful!"
          echo "✅ Application is running"
          echo "🌐 URL: https://atenmed.com.br"

      - name: Notify Failure
        if: failure()
        run: |
          echo "❌ Deploy failed!"
          echo "Check logs for details"
